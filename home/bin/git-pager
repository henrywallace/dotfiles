#!/bin/bash

# https://github.com/dandavison/delta
# if command -v delta &>/dev/null; then
#   exec delta --hunk-style=plain
# if command -v bat &>/dev/null || false; then
#   exec bat -l diff -p
# else
#   exec less
# fi
exec less
