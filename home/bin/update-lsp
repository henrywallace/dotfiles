#!/bin/bash

set -ex

up() {
	if command -v brew >&/dev/null; then
		brew upgrade "$@"
	elif command -v paru >&/dev/null; then
		paru -S --needed "$@"
	fi
}

up \
	lua-language-server\
	gopls \
	pyright \
	diagnostic-languageserver \
	rust-analyzer \
	paru:vint-git \
